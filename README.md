# text-classification-tensorflow
A deep learning project that performs sentiment analysis on movie reviews using TensorFlow, TensorFlow Hub, and TensorFlow Datasets. The model uses pretrained word embeddings to classify text as positive or negative and saves evaluation results to a file for documentation.
